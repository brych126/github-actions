name: Run DB Integration Tests

on:
  workflow_dispatch:

jobs:
  db-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Check for sqlcmd (pre-check)
        run: command -v sqlcmd || echo "sqlcmd not found"