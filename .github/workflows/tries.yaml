on: workflow_dispatch

defaults:
  run:
    shell: bash
    working-directory: ./testDir

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pwd                 
      - run: ls                  